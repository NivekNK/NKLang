main: func() {
    println("Hello World!");
}

main: func()! {
    error_early_return()!;
}

main: func(args: string[], args_count: u32) {
    for i := 0; i < args_count; i++ {
        println(args[i]);
    }
}

main: func(args: string[]) {
    for arg := args {
        println(arg);
    }
}
